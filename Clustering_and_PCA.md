# 군집화 및 차원 축소 (Clustering & PCA)

## 1. 군집화·차원 축소의 정의 및 배경

- **정의**: 군집화는 레이블 없이 유사한 데이터를 그룹으로 묶는 비지도학습이며, 차원 축소(PCA)는 정보 손실을 최소화하며 고차원 데이터를 저차원으로 압축하는 기법임.
- **배경/필요성**:
  - 레이블 없는 데이터에서 내재된 구조·군집을 발견할 필요가 있음.
  - 고차원 데이터의 '차원의 저주'를 완화할 필요가 있음.
  - 시각화·연산 효율·과적합 완화를 위한 특징 압축이 요구됨.

## 2. 군집화·차원 축소의 아키텍처 및 핵심 요소

### 가. 개념도/메커니즘 (Concept Diagram)

- **메커니즘 설명**: K-means는 군집 중심(Centroid)과 데이터 간 거리를 반복 최소화하여 군집을 형성하고, PCA는 데이터 분산이 최대인 주성분 축으로 좌표를 투영하여 차원을 축소함.

```
K-means: 중심 초기화 -> 할당 -> 중심 재계산 -> 수렴까지 반복
PCA: 고차원 데이터 --분산 최대 주성분(PC1,PC2) 투영--> 저차원
        ↑PC2
        |  ●●        ← 분산 최대 방향으로 축 회전
        +----→ PC1
```

### 나. 핵심 구성 요소 및 기술 (Key Components)

| 구분 | 핵심 기법 | 상세 설명 | 비고/사례 |
| :--- | :--- | :--- | :--- |
| 분할 군집 | K-means | k개 중심 기반 거리 최소화 반복 | k 사전 지정 |
| 계층 군집 | Hierarchical | 병합·분할로 군집 계층 구성 | 덴드로그램 |
| 밀도 군집 | DBSCAN | 밀도 기반, 임의 형태·이상치 탐지 | k 불필요 |
| 차원 축소 | PCA | 분산 최대 주성분으로 투영 | 고유값 분해 |
| 비선형 축소 | t-SNE/UMAP | 국소 구조 보존 시각화 | 탐색적 분석 |

## 3. K-means 군집화와 PCA 비교 분석

| 항목 | K-means (군집화) | PCA (차원 축소) |
| :--- | :--- | :--- |
| **목적** | 데이터를 군집으로 분할 | 특징 차원을 압축 |
| **결과** | 군집 레이블 | 주성분 좌표 |
| **주요 특징** | 거리 기반 반복 할당 | 분산 최대 축 투영 |
| **적용 분야** | 고객 세분화·이상탐지 | 시각화·전처리·잡음 제거 |

## 4. 정보관리기술사 관점의 활용 방안 및 향후 전망 (Insight)

- **도입/활용 시 고려사항**: K-means는 군집 수 k와 초기값에 민감하므로 엘보우·실루엣 기법으로 k를 결정하고, PCA는 스케일 정규화를 선행해야 함.
- **향후 전망/기술 트렌드**: 오토인코더 기반 비선형 차원 축소·표현 학습(Representation Learning)으로 고도화되는 추세임.
