# 탐색 알고리즘 (Search Algorithms)

## 1. 탐색 알고리즘의 정의 및 배경

- **정의**: 상태 공간(State Space)에서 초기 상태로부터 목표 상태에 이르는 경로를 체계적으로 찾는 문제 해결 기법임.
- **배경/필요성**:
  - 경로 탐색·퍼즐·게임 등 문제를 상태 공간 탐색으로 정형화할 필요가 있음.
  - 탐색 공간이 방대하여 효율적 탐색 전략이 요구됨.
  - 목표까지의 비용을 최소화하는 최적 경로 확보가 필요함.

## 2. 탐색 알고리즘의 아키텍처 및 핵심 요소

### 가. 개념도/메커니즘 (Concept Diagram)

- **메커니즘 설명**: 탐색은 사전 정보 없이 탐색하는 **맹목적 탐색(Uninformed)** 과 휴리스틱으로 방향을 잡는 **정보 이용 탐색(Informed)** 으로 구분됨. A* 는 실제 비용 g(n)과 추정 비용 h(n)의 합으로 경로를 평가함.

$$f(n) = g(n) + h(n)$$

```
           [초기 상태]
          /     |     \
       [A]     [B]     [C]      맹목적: 순서대로 확장(BFS/DFS)
      / \      / \             정보이용: f(n) 최소 노드 우선(A*)
   [목표]  ...        ...
```

### 나. 핵심 구성 요소 및 기술 (Key Components)

| 구분 | 핵심 기법 | 상세 설명 | 비고/사례 |
| :--- | :--- | :--- | :--- |
| 맹목적 | BFS (너비우선) | 얕은 노드부터 계층적 확장, 최단 경로 보장 | 큐(FIFO) |
| 맹목적 | DFS (깊이우선) | 한 경로를 끝까지 탐색 | 스택(LIFO)·메모리 효율 |
| 정보이용 | 그리디 탐색 | 휴리스틱 $h(n)$만으로 목표 근접 우선 | 빠르나 비최적 |
| 정보이용 | A* 알고리즘 | $f(n)=g(n)+h(n)$로 최적 경로 탐색 | 허용적 휴리스틱 시 최적 |
| 최적화 | 지역 탐색 | 현재 상태 주변만 탐색(언덕오르기) | 유전·담금질 |

## 3. 맹목적 탐색과 정보 이용 탐색 비교 분석

| 항목 | 맹목적 탐색 (Uninformed) | 정보 이용 탐색 (Informed) |
| :--- | :--- | :--- |
| **개념** | 사전 정보 없이 전수 탐색 | 휴리스틱으로 방향 유도 |
| **주요 특징** | BFS·DFS | 그리디·A* |
| **장단점** | 단순·완전성 / 비효율 | 효율적 / 휴리스틱 설계 필요 |
| **적용 분야** | 소규모 상태 공간 | 대규모·경로 탐색 |

## 4. 정보관리기술사 관점의 활용 방안 및 향후 전망 (Insight)

- **도입/활용 시 고려사항**: 휴리스틱이 실제 비용을 초과하지 않는 허용성(Admissibility)을 만족해야 A* 의 최적성이 보장됨.
- **향후 전망/기술 트렌드**: 탐색과 학습을 결합한 몬테카를로 트리 탐색(MCTS)이 게임·의사결정 AI의 핵심 기법으로 활용됨.
