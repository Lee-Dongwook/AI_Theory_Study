# 활성 함수 및 기울기 소실 (Activation Functions & Vanishing Gradient)

## 1. 정의 및 배경

- **정의**: 활성 함수는 뉴런의 가중합에 비선형성을 부여하는 함수이며, 기울기 소실은 역전파 시 기울기가 0에 수렴해 학습이 정체되는 현상임.
- **배경/필요성**:
  - 비선형 활성 함수가 없으면 다층 신경망도 선형 모델에 불과함.
  - 심층 신경망에서 기울기가 소실되어 학습이 진행되지 않는 문제 해결이 필요함.
  - 층 깊이에 무관한 안정적 학습 수단이 요구됨.

## 2. 아키텍처 및 핵심 요소

### 가. 개념도/메커니즘 (Concept Diagram)

- **메커니즘 설명**: 시그모이드·tanh는 입력이 크거나 작을 때 기울기가 0에 가까워져, 역전파 시 층을 거칠수록 기울기가 곱해지며 소실됨. ReLU는 양수 구간 기울기를 1로 유지해 이를 완화함.

```
Sigmoid: ___----1   (양끝 기울기≈0 → 소실)     ReLU: 0 for x<0, x for x≥0
        /                                             /
     __/  0                                     _____/  (양수 기울기 1)
```

### 나. 핵심 구성 요소 및 기술 (Key Components)

| 구분         | 활성 함수        | 상세 설명                  | 비고/사례      |
| :----------- | :--------------- | :------------------------- | :------------- |
| 시그모이드   | Sigmoid          | 0~1 출력, 확률 해석        | 기울기 소실    |
| 하이퍼탄젠트 | tanh             | -1~1 출력, 영점 중심       | 소실 잔존      |
| ReLU         | Rectified Linear | 음수는 0, 양수는 그대로    | 소실 완화·표준 |
| Leaky ReLU   | 변형 ReLU        | 음수 구간 소량 기울기 부여 | 죽은 ReLU 방지 |
| 소프트맥스   | Softmax          | 다중 클래스 확률 분포 출력 | 분류 출력층    |

## 3. 시그모이드와 ReLU 비교 분석

| 항목            | 시그모이드 (Sigmoid) | ReLU                 |
| :-------------- | :------------------- | :------------------- |
| **출력 범위**   | 0 ~ 1                | 0 ~ ∞                |
| **기울기 소실** | 발생 (양끝 포화)     | 완화 (양수 기울기 1) |
| **연산 비용**   | 지수 연산·높음       | 단순·낮음            |
| **적용 위치**   | 출력층(이진)         | 은닉층 표준          |

## 4. 정보관리기술사 관점의 활용 방안 및 향후 전망 (Insight)

- **도입/활용 시 고려사항**: 은닉층은 ReLU 계열을 기본으로 사용하고, 가중치 초기화(He/Xavier)·배치 정규화·잔차 연결로 기울기 소실을 종합 대응해야 함.
- **향후 전망/기술 트렌드**: GELU·Swish 등 부드러운 활성 함수가 대규모 모델에서 활용되며, 활성 함수 선택이 성능 최적화 요소로 다뤄짐.
