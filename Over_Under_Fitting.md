## 오버피팅 (Overfitting)
학습 데이터에 성능이 좋지만 실제 데이터에 관해 성능이 떨어지는 현상

### 발생 원인
- 훈련 데이터를 과하게 학습
- 모델의 복잡성
- 파라미터 갯수의 과다


## 언더피팅 (Underfitting)
적정 수준의 학습을 하지 못해 실제 성능이 떨어지는 현상

## 발생 원인
- 학습 부족, 학습 데이터 부족
- 모델의 단순화
- 파라미터 갯수가 부족함

---

머신러닝의 지향점 : AI의 모델을 학습하는 과정에서 모델의 강건성 (Robustness)을 유지하는 것이 중요 - 적정 수준 학습


### 판단 근거

1. 과대적합 모델 (오버피팅)
- 분산이 크고 편향이 작다
- 훈련 데이터를 수행할 때는 에러율이 작다 (편향이 작음)
- 실제 데이터를 수행할 때는 예측값과 실제값의 차이가 크다 (분산이 큼)
- 학습 데이터의 학습 횟수가 증가할수록, 모델의 오류는 낮아지지만, 테스트 데이터에 대해서는 어느순간 오류가 높아진다 (과적합 현상)
- 어느 순간부터 테스트 데이터에 대한 정확도가 낮아짐

2. 과소적합 모델 (언더피팅)
- 분산이 작고 편향이 크다
- 실제 데이터를 수행할 때는 모델 복잡도가 낮기 때문에 예측값과 실제값의 차이가 적다 (분산이 작음)
- 훈련 데이터를 수행할때는 오차율이 크다 (편향이 큼)
- 학습 초기에, 학습 데이터와 테스트 데이터에서 모두 높은 오류율을 보인다.

---

### 해결 방법
1. 오버피팅 해결
   - 규제화 (Regularization) : 데이터의 일정한 규칙에 따라 변형하여 가중치의 값을 0으로 만들거나 줄이는 방법
     - L2 규제 (L2 norm) : 독립변수 가중치의 크기를 0에 근접하도록 규제 적용, 가중치의 제곱값 사용
     - L1 규제 (L1 norm) : 독립변수 가중치를 0으로 만드는 규제 적용, 가중치의 절대값 사용
   - 교차 검증 (Cross Validation) : 원본 데이터를 학습/검증/테스트 데이터로 분할 (Hold-out) (k-fold CV) (Leave-One-Out CV)
   - 충분한 데이터 확보
   - 사전 학습 (Pre-Training) : 초기 가중치 값을 비 지도 학습을 이용하거나 기 학습된 모델을 재사용하여 정확도를 높인다. (RBM 제한된 볼츠만 머신) (Auto-Encoder) (전이학습)
   - 차원 축소 : 각 독립변수의 의미를 유지하면서 대표성을 띄는 형태로 데이터 압축한다. / PCA, LDA, 요인분석, MDS
   - 드롭아웃 (DropOut) : 전체 노드의 가중치 중 레이어에 포함된 주요 노드 가중치의 일부만 연산에 참여할 수 있도록 하여, 불필요한 노드를 삭제한다.
   - 배치 정규화 (Batch Normalization) : 모델 학습 과정에서 각 학습 단위로 데이터가 다양한 분포를 가지더라도 각 배치별 평균과 분산을 이용하여 범위를 벗어나지 않도록 값을 제한한다.
   - 조기종료 (Early Stopping) : 학습을 반복하다가 검증 데이터 정확도가 더 이상 상승하지 않을때 종료한다 /. 의사결정트리의 정지규칙 depth 결정
     
2. 언더피팅 해결
   - 충분한 데이터 확보 : 모델에 적합한 충분한 독립 변수 및 데이터를 추가하여 모델이 학습할 수 있는 범위, 즉 분산을 증가시킨다.
   - 사전 학습 (Pre-Training) : RBM, Auto-Encoder를 이용하여 초기 가중치 값을 업데이트 한다.
   - 학습 모델 변경 : 높은 분산을 가지는 모델인 의사결정트리, K-NN, SVM 모델을 사용한다
   - 기울기 소멸 문제 해결 : 시그모이드 활성 함수를 렐루(ReLU)함수로 대체한다


**출처** : 빅데이터 분석기사(아이리포, 최우슬/최다정 저)
