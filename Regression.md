# 회귀 분석 (Regression Analysis)

## 1. 회귀 분석의 정의 및 배경

- **정의**: 독립변수와 종속변수 간의 관계를 함수로 모델링하여 연속 값(선형 회귀) 또는 확률·범주(로지스틱 회귀)를 예측하는 지도학습 기법임.
- **배경/필요성**:
  - 변수 간 관계를 정량화하여 예측·추세 분석에 활용할 필요가 있음.
  - 연속형 예측과 분류(확률) 문제를 통계적으로 다룰 필요가 있음.
  - 예측 근거를 계수(가중치)로 해석할 수 있어야 함.

## 2. 회귀 분석의 아키텍처 및 핵심 요소

### 가. 개념도/메커니즘 (Concept Diagram)

- **메커니즘 설명**: 선형 회귀는 오차 제곱합을 최소화하는 직선을 적합하고, 로지스틱 회귀는 선형 결합을 시그모이드 함수로 변환해 0~1 확률로 사상함.

```
선형 회귀: y = w·x + b        로지스틱: p = σ(w·x + b), σ(z)=1/(1+e^-z)
  y|      ●  ●/                 p|      ___----1
   |   ● /● ●                    |     /
   |  /● ●                       | ___/  0
   +-----------> x               +-----------> z
```

### 나. 핵심 구성 요소 및 기술 (Key Components)

| 구분 | 핵심 요소 | 상세 설명 | 비고/사례 |
| :--- | :--- | :--- | :--- |
| 선형 회귀 | 최소제곱법 | 오차 제곱합(MSE) 최소화로 계수 추정 | 연속값 예측 |
| 로지스틱 회귀 | 시그모이드 | 선형 결합을 확률(0~1)로 변환 | 이진 분류 |
| 손실 함수 | MSE / 교차엔트로피 | 회귀는 MSE, 분류는 로그 손실 | 학습 목표 |
| 규제화 | L1/L2 | 계수 크기 제약으로 과적합 방지 | Ridge/Lasso |

## 3. 선형 회귀와 로지스틱 회귀 비교 분석

| 항목 | 선형 회귀 (Linear) | 로지스틱 회귀 (Logistic) |
| :--- | :--- | :--- |
| **개념** | 연속 값 예측 | 범주(확률) 예측 |
| **출력** | 실수 값 | 0~1 확률 |
| **손실 함수** | 평균제곱오차(MSE) | 교차 엔트로피 |
| **적용 분야** | 수요·가격 예측 | 분류(스팸·이탈) |

## 4. 정보관리기술사 관점의 활용 방안 및 향후 전망 (Insight)

- **도입/활용 시 고려사항**: 다중공선성·이상치·비선형성을 진단하고, 규제화로 과적합을 제어해야 함.
- **향후 전망/기술 트렌드**: 회귀는 해석 가능성이 높아 설명 가능 AI·기준 모델(Baseline)로 지속 활용되며, 일반화 선형 모델(GLM)로 확장됨.
