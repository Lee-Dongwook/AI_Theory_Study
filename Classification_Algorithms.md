# 주요 분류 알고리즘 (SVM·KNN·Naive Bayes)

## 1. 분류 알고리즘의 정의 및 배경

- **정의**: 입력 데이터를 미리 정의된 범주(클래스)로 구분하는 지도학습 알고리즘으로, SVM·KNN·나이브 베이즈가 대표적임.
- **배경/필요성**:
  - 데이터 분포·규모·해석 요구에 따라 상이한 분류 기법 선택이 필요함.
  - 선형·비선형 경계, 거리 기반, 확률 기반 등 다양한 접근이 요구됨.
  - 고차원·소량 데이터 등 조건별 최적 알고리즘 이해가 필요함.

## 2. 분류 알고리즘의 아키텍처 및 핵심 요소

### 가. 개념도/메커니즘 (Concept Diagram)

- **메커니즘 설명**: SVM은 클래스 간 마진을 최대화하는 초평면을, KNN은 최근접 이웃의 다수결을, 나이브 베이즈는 조건부 확률을 기반으로 분류함.

```
 SVM: 마진 최대 초평면        KNN: 최근접 k개 다수결
   ○ ○ | △ △                    ● ← 이웃 3개 중 △2:○1 => △
   ○ ○ |△ △                  나이브 베이즈: argmax P(C)∏P(x|C)
       ↑최대 마진
```

### 나. 핵심 구성 요소 및 기술 (Key Components)

| 구분      | 알고리즘      | 상세 설명                                     | 비고/사례   |
| :-------- | :------------ | :-------------------------------------------- | :---------- |
| 마진 기반 | SVM           | 마진 최대 초평면으로 분류, 커널로 비선형 대응 | 커널 트릭   |
| 거리 기반 | KNN           | 최근접 k개 이웃의 다수결로 분류               | 게으른 학습 |
| 확률 기반 | 나이브 베이즈 | 특징 독립 가정하 조건부 확률 최대 클래스 선택 | 텍스트 분류 |

## 3. SVM·KNN·나이브 베이즈 비교 분석

| 항목          | SVM                | KNN                   | 나이브 베이즈    |
| :------------ | :----------------- | :-------------------- | :--------------- |
| **원리**      | 마진 최대 초평면   | 최근접 이웃 다수결    | 조건부 확률      |
| **학습 방식** | 경계 최적화        | 게으른 학습(저장)     | 확률 추정        |
| **장점**      | 고차원·비선형 강함 | 단순·직관적           | 빠름·소량 데이터 |
| **단점**      | 대용량 느림·튜닝   | 예측 느림·차원의 저주 | 독립 가정 비현실 |

## 4. 정보관리기술사 관점의 활용 방안 및 향후 전망 (Insight)

- **도입/활용 시 고려사항**: 데이터 규모·차원·특징 독립성 등을 진단하여 알고리즘을 선택하고, KNN은 정규화·차원 축소를 병행해야 함.
- **향후 전망/기술 트렌드**: 전통적 분류기는 해석 용이성·경량성으로 여전히 유효하며, 앙상블·딥러닝과 결합해 활용됨.
