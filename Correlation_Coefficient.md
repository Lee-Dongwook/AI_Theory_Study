# 상관계수 (Correlation Coefficient)

## 1. 상관계수의 정의 및 배경

- **정의**: 두 변수 간 연관성의 방향과 강도를 $-1 \le r \le 1$ 범위의 수치로 계량화한 통계 지표임.
- **배경/필요성**:
  - 변수 간 선형·순위 관계의 존재 여부와 강도를 객관적으로 판단할 필요가 있음.
  - 데이터의 척도(등간·서열)와 분포 특성에 따라 적합한 지표 선택이 요구됨.
  - 피처 선택(Feature Selection), 다중공선성 진단 등 데이터 분석 전처리의 기반이 됨.

## 2. 상관계수의 아키텍처 및 핵심 기술 요소

### 가. 개념도/메커니즘 (Concept Diagram)

- **메커니즘 설명**: 두 변수의 공분산(함께 변하는 정도)을 각 변수의 표준편차 곱으로 나누어 척도에 무관하게 정규화한 값이 상관계수임. $r=+1$은 완전 양의 선형, $r=-1$은 완전 음의 선형, $r=0$은 무상관을 의미함.

```
r < 0  (음의 상관)      r ≈ 0  (무상관)      r > 0  (양의 상관)
  \                        · ·  ·              /
   \                      ·  · ·              /
    \                      · · ·             /
```

### 나. 핵심 구성 요소 및 기술 (Key Components)

| 구분           | 핵심 지표         | 산출식                                                                                              | 비고/사례                        |
| :------------- | :---------------- | :-------------------------------------------------------------------------------------------------- | :------------------------------- |
| 공분산         | $\text{Cov}(X,Y)$ | $\dfrac{\sum (X_i - \bar{X})(Y_i - \bar{Y})}{n-1}$                                                  | 척도 의존, 정규화 이전 단계      |
| 피어슨(모집단) | $\rho_{X,Y}$      | $\dfrac{\text{Cov}(X,Y)}{\sigma_X \sigma_Y}$                                                        | 선형 연관성, 등간·비율 척도      |
| 피어슨(표본)   | $r$               | $\dfrac{\sum (X_i - \bar{X})(Y_i - \bar{Y})}{\sqrt{\sum (X_i - \bar{X})^2 \sum (Y_i - \bar{Y})^2}}$ | 표본 기반 추정                   |
| 스피어먼       | $\rho$            | $1 - \dfrac{6 \sum d_i^2}{n(n^2 - 1)}$                                                              | 순위 기반 비모수 ($d_i$: 순위차) |

## 3. 피어슨과 스피어먼 상관계수 비교 분석

| 항목          | 피어슨 (Pearson)               | 스피어먼 (Spearman)                    |
| :------------ | :----------------------------- | :------------------------------------- |
| **개념**      | 두 변수의 선형적 연관성 계량화 | 두 변수의 순위 자료 기반 연관성 계량화 |
| **주요 특징** | 모수적, 실제 값 사용           | 비모수적, 순위 값 사용                 |
| **장단점**    | 선형관계 정밀 / 이상치에 민감  | 이상치·비선형에 강건 / 정보 손실       |
| **적용 분야** | 등간·비율 척도, 선형 관계      | 서열 척도, 비선형·단조 관계            |

## 4. 정보관리기술사 관점의 활용 방안 및 향후 전망 (Insight)

- **도입/활용 시 고려사항**: 상관관계가 인과관계를 의미하지 않음에 유의해야 함. 데이터의 척도와 분포를 먼저 진단하여 지표를 선택해야 함.
- **향후 전망/기술 트렌드**: 비선형 종속성을 포착하는 상호정보량(Mutual Information), 거리 상관(Distance Correlation) 등으로 상관 분석이 고도화되는 추세임.
