# 퍼셉트론·MLP·역전파 (Perceptron, MLP & Backpropagation)

## 1. 정의 및 배경

- **정의**: 퍼셉트론은 인공 뉴런의 기본 단위이며, 다층 퍼셉트론(MLP)은 은닉층을 쌓은 신경망, 역전파는 출력 오차를 역방향으로 전파해 가중치를 갱신하는 학습 알고리즘임.
- **배경/필요성**:
  - 단층 퍼셉트론은 XOR 등 비선형 문제를 풀 수 없어 다층 구조가 필요함.
  - 다층 신경망의 방대한 가중치를 효율적으로 학습할 방법이 요구됨.
  - 오차의 원인을 각 가중치에 분배하는 체계적 기법이 필요함.

## 2. 아키텍처 및 핵심 요소

### 가. 개념도/메커니즘 (Concept Diagram)

- **메커니즘 설명**: 순전파로 입력→은닉→출력을 계산하고, 출력 오차를 연쇄 법칙(Chain Rule)으로 역방향 전파하여 각 가중치의 기울기를 구한 뒤 경사 하강법으로 갱신함.

```
순전파 →   [입력층] → [은닉층] → [출력층] → 손실
역전파 ←   가중치 갱신 ← 기울기 전파(연쇄법칙) ← 오차
```

### 나. 핵심 구성 요소 및 기술 (Key Components)

| 구분          | 핵심 요소       | 상세 설명                      | 비고/사례   |
| :------------ | :-------------- | :----------------------------- | :---------- |
| 퍼셉트론      | 인공 뉴런       | 가중합 + 활성함수로 출력 산출  | 선형 분리   |
| 다층 퍼셉트론 | MLP             | 은닉층으로 비선형 문제 해결    | XOR 해결    |
| 순전파        | Forward         | 입력→출력 방향 계산            | 예측 산출   |
| 역전파        | Backpropagation | 연쇄 법칙으로 기울기 역전파    | 학습 핵심   |
| 가중치 갱신   | 경사 하강       | 기울기 반대 방향 파라미터 조정 | 손실 최소화 |

## 3. 단층 퍼셉트론과 다층 퍼셉트론 비교 분석

| 항목       | 단층 퍼셉트론       | 다층 퍼셉트론 (MLP) |
| :--------- | :------------------ | :------------------ |
| **개념**   | 입력-출력 단일 계층 | 은닉층 포함 다계층  |
| **표현력** | 선형 분리만 가능    | 비선형 문제 해결    |
| **한계**   | XOR 해결 불가       | 기울기 소실 가능    |
| **학습**   | 단순 갱신           | 역전파 필요         |

## 4. 정보관리기술사 관점의 활용 방안 및 향후 전망 (Insight)

- **도입/활용 시 고려사항**: 층이 깊어질수록 기울기 소실·과적합 위험이 커지므로 활성함수·정규화·규제화를 병행해야 함.
- **향후 전망/기술 트렌드**: 역전파는 모든 현대 딥러닝 학습의 근간으로, 자동 미분(Autograd) 프레임워크로 구현이 자동화됨.
