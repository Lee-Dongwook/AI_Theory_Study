# 합성곱 신경망 (CNN, Convolutional Neural Network)

## 1. CNN의 정의 및 배경

- **정의**: 합성곱(Convolution)과 풀링(Pooling) 연산으로 이미지의 공간적 특징을 계층적으로 추출하는 딥러닝 신경망임.
- **배경/필요성**:
  - 이미지를 완전연결 신경망으로 처리 시 파라미터가 폭증하는 문제 해결이 필요함.
  - 위치가 변해도 동일 특징을 인식하는 이동 불변성이 요구됨.
  - 저수준(엣지)→고수준(형태) 특징의 계층적 학습이 필요함.

## 2. CNN의 아키텍처 및 핵심 요소

### 가. 개념도/메커니즘 (Concept Diagram)

- **메커니즘 설명**: 필터(커널)가 입력을 슬라이딩하며 지역 특징을 추출(합성곱)하고, 풀링으로 공간 크기를 축소해 불변성·효율을 확보한 뒤, 완전연결층에서 분류함.

```
[입력 이미지] -> [합성곱+ReLU] -> [풀링] -> ... -> [완전연결층] -> [분류]
   (지역 특징 추출)   (차원 축소)          (판별)
```

### 나. 핵심 구성 요소 및 기술 (Key Components)

| 구분            | 구성 요소      | 상세 설명                          | 비고/사례      |
| :-------------- | :------------- | :--------------------------------- | :------------- |
| 합성곱층        | Convolution    | 필터로 지역 특징 추출, 가중치 공유 | 파라미터 절감  |
| 필터/커널       | Filter         | 엣지·질감 등 특징 검출             | 학습 대상      |
| 풀링층          | Pooling        | 최대·평균으로 공간 축소            | 이동 불변성    |
| 스트라이드/패딩 | Stride/Padding | 이동 간격·경계 처리                | 출력 크기 조절 |
| 완전연결층      | FC Layer       | 추출 특징을 최종 분류              | 출력 산출      |

## 3. CNN과 완전연결 신경망(FNN) 비교 분석

| 항목            | CNN                        | 완전연결 신경망 (FNN) |
| :-------------- | :------------------------- | :-------------------- |
| **연결 방식**   | 지역 연결·가중치 공유      | 전 노드 완전 연결     |
| **파라미터 수** | 적음                       | 많음(폭증)            |
| **특징**        | 공간 구조·이동 불변성 보존 | 공간 정보 소실        |
| **적용 분야**   | 이미지·영상                | 정형·저차원 데이터    |

## 4. 정보관리기술사 관점의 활용 방안 및 향후 전망 (Insight)

- **도입/활용 시 고려사항**: 데이터 부족 시 전이학습(사전학습 CNN)·데이터 증강을 활용해야 함.
- **향후 전망/기술 트렌드**: 이미지 분야에서 비전 트랜스포머(ViT)와 상호 보완·경쟁하며, 의료·자율주행 등 비전 응용의 근간으로 활용됨.
