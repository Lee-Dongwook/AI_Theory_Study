# 모델 평가 (Model Evaluation)

## 1. 모델 평가의 정의 및 배경

- **정의**: 학습된 모델의 예측 성능·일반화 능력을 혼동 행렬·평가 지표·검증 기법으로 정량 측정하는 과정임.
- **배경/필요성**:
  - 학습 데이터가 아닌 미지의 데이터에 대한 일반화 성능 확인이 필요함.
  - 문제 특성(불균형·오탐 비용)에 맞는 지표 선택이 요구됨.
  - 편향-분산 트레이드오프를 진단하여 모델을 개선할 필요가 있음.

## 2. 모델 평가의 아키텍처 및 핵심 요소

### 가. 개념도/메커니즘 (Concept Diagram)

- **메커니즘 설명**: 혼동 행렬(TP/FP/FN/TN)에서 정확도·정밀도·재현율·F1을 산출하고, 교차 검증으로 데이터 분할에 따른 성능 편차를 완화하여 신뢰도를 확보함.

```
               예측 P    예측 N
   실제 P   [  TP  ] [  FN  ]   재현율 = TP/(TP+FN)
   실제 N   [  FP  ] [  TN  ]   정밀도 = TP/(TP+FP)
```

### 나. 핵심 구성 요소 및 기술 (Key Components)

| 구분 | 핵심 지표 | 산출/개념 | 비고/사례 |
| :--- | :--- | :--- | :--- |
| 정확도 | Accuracy | $(TP+TN)/전체$ | 불균형 시 왜곡 |
| 정밀도 | Precision | $TP/(TP+FP)$ | 오탐 비용 중요 시 |
| 재현율 | Recall | $TP/(TP+FN)$ | 미탐 비용 중요 시 |
| F1 점수 | F1-score | 정밀도·재현율의 조화평균 | 불균형 데이터 |
| ROC/AUC | 곡선·면적 | 임계값별 TPR-FPR 곡선 아래 면적 | 분류기 비교 |
| 검증 | 교차 검증 | k-fold 등으로 일반화 성능 추정 | 과적합 진단 |

## 3. 정밀도와 재현율 비교 분석

| 항목 | 정밀도 (Precision) | 재현율 (Recall) |
| :--- | :--- | :--- |
| **개념** | 예측 양성 중 실제 양성 비율 | 실제 양성 중 예측 양성 비율 |
| **초점** | 오탐(FP) 최소화 | 미탐(FN) 최소화 |
| **트레이드오프** | 임계값↑ 시 상승 | 임계값↑ 시 하락 |
| **적용 분야** | 스팸 필터 | 암 진단·이상 탐지 |

## 4. 정보관리기술사 관점의 활용 방안 및 향후 전망 (Insight)

- **도입/활용 시 고려사항**: 데이터 불균형 시 정확도 대신 F1·AUC를 사용하고, 오탐·미탐의 비즈니스 비용에 따라 지표를 선택해야 함.
- **향후 전망/기술 트렌드**: 성능 지표를 넘어 공정성(Fairness)·강건성(Robustness)·설명 가능성을 포함한 다면적 모델 검증으로 확장되는 추세임.
