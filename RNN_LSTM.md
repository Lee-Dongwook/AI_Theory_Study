# 순환 신경망 (RNN / LSTM / GRU)

## 1. 정의 및 배경

- **정의**: RNN은 이전 시점의 은닉 상태를 현재 입력과 결합해 순차 데이터를 처리하는 신경망이며, LSTM·GRU는 게이트 구조로 장기 의존성을 개선한 변형임.
- **배경/필요성**:
  - 시계열·문장 등 순서가 있는 데이터의 시간적 의존성 처리가 필요함.
  - 기본 RNN의 장기 의존성 학습 실패(기울기 소실) 해결이 요구됨.
  - 과거 정보의 선택적 기억·망각 메커니즘이 필요함.

## 2. 아키텍처 및 핵심 요소

### 가. 개념도/메커니즘 (Concept Diagram)

- **메커니즘 설명**: RNN은 은닉 상태를 다음 시점으로 순환 전달하며, LSTM은 셀 상태와 입력·망각·출력 게이트로 정보를 선택적으로 유지·제거하여 장기 의존성을 확보함.

```
RNN:  x1 -> [h1] -> [h2] -> [h3] -> 출력   (h: 은닉 상태 순환 전달)
LSTM: 셀 상태 ──[망각 게이트]─[입력 게이트]─[출력 게이트]──> 다음 시점
```

### 나. 핵심 구성 요소 및 기술 (Key Components)

| 구분        | 구성 요소    | 상세 설명                    | 비고/사례      |
| :---------- | :----------- | :--------------------------- | :------------- |
| 은닉 상태   | Hidden State | 과거 정보를 요약해 순환 전달 | 순차 기억      |
| 셀 상태     | Cell State   | 장기 정보를 보존하는 경로    | LSTM 핵심      |
| 망각 게이트 | Forget Gate  | 불필요 과거 정보 제거        | 선택적 망각    |
| 입력 게이트 | Input Gate   | 새 정보의 반영량 조절        | 선택적 기억    |
| GRU         | 간소화 구조  | 게이트 통합으로 경량화       | LSTM 대비 효율 |

## 3. RNN과 LSTM 비교 분석

| 항목            | 기본 RNN               | LSTM                 |
| :-------------- | :--------------------- | :------------------- |
| **구조**        | 단순 순환              | 셀 상태 + 3개 게이트 |
| **장기 의존성** | 학습 실패(기울기 소실) | 장기 의존성 유지     |
| **연산 비용**   | 낮음                   | 높음(파라미터 多)    |
| **적용 분야**   | 짧은 시퀀스            | 긴 시퀀스(번역·음성) |

## 4. 정보관리기술사 관점의 활용 방안 및 향후 전망 (Insight)

- **도입/활용 시 고려사항**: 긴 시퀀스는 LSTM·GRU를 사용하되, 순차 연산으로 병렬화가 어려운 한계를 고려해야 함.
- **향후 전망/기술 트렌드**: 병렬 처리·장거리 의존성에 유리한 Transformer가 다수 순차 처리 영역을 대체하는 추세임.
