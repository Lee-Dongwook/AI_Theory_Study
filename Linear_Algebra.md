# 선형대수 기초 (Linear Algebra for AI)

## 1. 선형대수의 정의 및 배경

- **정의**: 벡터·행렬·선형변환을 다루는 수학 분야로, AI에서 데이터·가중치·연산을 수치적으로 표현·처리하는 기반 이론임.
- **배경/필요성**:
  - 대량의 데이터와 신경망 연산을 행렬 형태로 병렬 처리할 필요가 있음.
  - 특징 공간에서의 데이터 변환·차원 조작이 요구됨.
  - GPU 기반 고속 행렬 연산으로 딥러닝 성능을 확보할 필요가 있음.

## 2. 선형대수의 핵심 요소

### 가. 개념도/메커니즘 (Concept Diagram)

- **메커니즘 설명**: 데이터는 벡터·행렬(텐서)로 표현되고, 가중치 행렬과의 곱(선형변환)으로 특징 공간이 변환됨. 신경망의 순전파는 행렬곱과 편향 합의 반복임.

```
[입력 벡터 x] --(가중치 행렬 W 곱)--> [Wx] --(편향 b 합)--> [Wx + b] --> 출력
```

### 나. 핵심 구성 요소 및 기술 (Key Components)

| 구분 | 핵심 요소 | 상세 설명 | 비고/사례 |
| :--- | :--- | :--- | :--- |
| 스칼라/벡터 | 0차/1차 | 단일 값 / 방향·크기를 가진 값의 배열 | 데이터 포인트 |
| 행렬/텐서 | 2차/n차 | 벡터의 배열 / 다차원 배열 | 가중치·이미지 |
| 행렬곱 | 선형변환 | 벡터를 다른 공간으로 사상 | 순전파 연산 |
| 고유값·고유벡터 | $Av = \lambda v$ | 변환에도 방향이 보존되는 벡터와 배율 | PCA 기반 |
| 내적/노름 | 유사도·크기 | 벡터 간 유사도·거리 계산 | 코사인 유사도 |

## 3. 벡터와 행렬 비교 분석

| 항목 | 벡터 (Vector) | 행렬 (Matrix) |
| :--- | :--- | :--- |
| **개념** | 1차원 수 배열 | 2차원 수 배열 |
| **주요 특징** | 하나의 데이터·방향 표현 | 다수 벡터·선형변환 표현 |
| **연산** | 내적·노름 | 행렬곱·전치·역행렬 |
| **적용** | 개별 샘플·임베딩 | 가중치·데이터셋·변환 |

## 4. 정보관리기술사 관점의 활용 방안 및 향후 전망 (Insight)

- **도입/활용 시 고려사항**: 고차원 행렬 연산은 계산 비용이 크므로 GPU·분산 연산 자원을 고려해야 함.
- **향후 전망/기술 트렌드**: 고유값 분해(SVD)·행렬 분해 기법이 차원 축소(PCA)·추천 시스템의 기반으로 지속 활용됨.
